# 建模原理、网络结构与训练方案

本目录对应问题3的第（1）项。`问题3_模型与实测结果说明.md` 给出 v5 的输入边界、逐位置跨模态结构、目标函数、训练配置、候选模型选择和部署偏置；`模型参数/` 保存正式部署组件和 manifest；`训练记录/` 保存各候选及种子的 history、result 和 checkpoint；`图表/` 保存训练曲线与模型比较图。

正式结果由 `模型参数/manifest.json` 指定的组件产生，不使用单个未经部署选择的候选 checkpoint 代替。
