# 验证集基础评价与错误归因

本目录对应问题3的第（5）项。`验证集评价/` 保存最终部署的 728 条预测、评价指标、混淆矩阵数据、候选比较、逐类别指标、错误分层和错误人工复核表；`验证集解释/` 保存同一部署模型的验证集解释；`图表/` 保存混淆矩阵、回归散点图和忠实度图。

`quality_audit.json` 显示预测、Shapley 重算、文本证据、验证指标和解释卡数量均通过数值审计，`交付状态.json` 的 `numerical_audit` 为 `passed`。错误人工复核表没有编造未实际回看的语义原因。正式 Macro-F1 为 `0.6432399504`，目标 `0.65` 未达到。
