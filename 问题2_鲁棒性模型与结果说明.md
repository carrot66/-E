# 问题2：缺失模态情感预测的建模与验证（当前实验版）

> 2026-09-23勘误：早期附件3表使用了错误的 `[UNK]` 可观测掩码，已经作废。附件3中 `[UNK]` 且语音、视觉同位置全零的131个位置必须判为共同缺失；最终逐条结果以 `outputs/问题2_固定文本融合附件3结果/问题2_附件3全量预测.csv` 为准。完整数据增训和固定文本融合已完成，旧附件2模型结果仅作历史对照。

这是一份基于实际运行结果的解题说明。附件2的 train、valid、test 分别为 3395、728、727 条；附件3为30条无标签样本。最终候选使用完整包 train 16,326 条拟合，完整包 valid 1,871 条选模；完整包 test、附件2 test 和附件3来源标签均未用于新候选的训练或选模。附件3没有真实标签，因此下表是预测结果，不是正确率。旧附件2 test 指标只保留作历史诊断，不能代表新候选的测试精度。

## 1. 鲁棒模型的原理、结构、目标函数和训练

### 数据审计与输入

每条记录的有效序列最多50个位置。`text_bert` 的三通道是 token ID、attention mask、token type；使用固定修订版 `google-bert/bert-base-uncased` 的最后隐状态得到词位文本向量768维。语音和视觉直接采用附件2/3给出的对齐特征，维度分别为74、35。模型训练、验证和附件3推理均使用同一个文本编码器与相同的标准化参数；标准化均值、标准差只从训练集可观测位置估计。

先用 `[CLS]` 和 `[SEP]` 确定真实内容范围，再分别建立文本、语音、视觉的可观测掩码。文本的 padding 与特殊标记不计入缺失率；A/V 内容位置上的全零向量被标记为不可观测，无法仅凭全零判断是人为遮挡还是原始提取失败。模拟文本缺失时，先将对应 token 替换为 `[MASK]` 并关闭其 attention，**随后才运行 BERT**，防止缺失词通过相邻上下文向量泄漏。三个模态的原始不可观测位置不参与融合。

机器审计发现：train/valid/test 中视觉整条不可观测分别有110/15/28条；达到50位置上限分别有253/47/52条。三个划分间样本 ID 与原视频 ID 均无重叠，`label.xlsx` 与特征文件的标签、文本、划分一致，没有发现非有限特征或极性与强度符号冲突。审计清单会标记可疑输入供人工看原素材；模型没有按预测结果改标签，也没有把这些困难样本静默删除。对数据中可能隐藏的问题，目前只能确认以上程序化检查和抽样错误分析，不能宣称已人工核验全部4850条。

### 网络与损失

三个模态各自经线性层投影到96维并归一化。对位置 $t$ 的模态 $m$，门控网络依据三个投影及可用掩码、整句可用比例得到分数 $g_{tm}$，在可用模态间归一化：

$$\alpha_{tm}=\frac{a_{tm}\exp(g_{tm})}{\sum_j a_{tj}\exp(g_{tj})},\qquad a_{tm}\in\{0,1\}. $$

融合向量连接三个加权投影及6维质量信息，再映射到96维。其后是一层4头 Transformer（前馈层192维），对有效时序位置做注意力池化，最后分出三分类头与情感强度回归头。回归输出经 $3\tanh(\cdot/3)$ 限制在 $[-3,3]$。当某位置三模态都不可用时，该位置不参加池化。

主要训练损失是频率平方根倒数加权的交叉熵加 $0.6$ 倍 Smooth-L1。普通融合基线对可用模态等权；“连续缺失增强”在约65%的训练样本上随机选择 T/A/V/TA/TV/AV/TAV，遮挡有效内容区间的10%、30%或50%；“动态门控”在增强之外学习上述门控；“门控蒸馏”进一步用干净输入训练好的普通融合模型作教师。蒸馏项为 $0.2T^2D_{KL}(p_T\Vert p_S)+0.1\operatorname{SmoothL1}(r_T,r_S)$，温度 $T=2$；仅当教师在训练样本上分类正确且强度绝对误差小于1时使用该软目标。该筛选只涉及训练集真实标签，不读取验证或测试标签进行蒸馏。

每组采用 AdamW，学习率 $3\times10^{-4}$、权重衰减 $10^{-3}$、batch size 64、dropout 0.2、梯度范数裁剪1、最多30轮，验证集提前停止耐心7轮。训练随机种子为2026、2027、2028，三模型概率与强度分别取均值。选模仅看验证集“完整输入 + T/A/V/AV/TAV 的30%连续缺失”；分数为完整与缺失 Macro-F1 的均值减去 $0.05$ 倍二者 MAE 之和。分类先取最大概率，最后按题目标签定义执行固定的一致性规则：中性强度设0，负向至多为−0.01，正向至少为0.01。该规则无可调阈值，所有最终评价与附件3输出使用同一规则。

### 消融与选模结果

| 三种子候选 | 验证选模分数↑ | 完整 Macro-F1↑ | 30%缺失平均 Macro-F1↑ | 完整 MAE↓ |
|---|---:|---:|---:|---:|
| 普通融合 | 0.5298 | 0.6007 | 0.5784 | 0.5882 |
| 连续缺失增强 | 0.5243 | 0.5887 | 0.5793 | 0.5897 |
| 动态门控 | 0.5452 | 0.6111 | 0.5959 | 0.5753 |
| 门控蒸馏 | **0.5539** | 0.6208 | **0.6049** | 0.5824 |
| 门控与蒸馏六模型平均 | 0.5533 | **0.6238** | 0.5994 | **0.5750** |

选门控蒸馏三种子模型，因为它在事先定义的综合分数与缺失情形 Macro-F1 上最好。连续缺失增强单独使用没有改善完整输入；门控和蒸馏共同带来验证集收益。还试过分层中性/极性头和仅微调 BERT 最后一层：分层三模型选模分数约0.5485，BERT 微调单模型最佳约0.5425，均未取代最终模型。分层加蒸馏六模型分数约0.5539、仅高于三模型约0.0007，缺失 F1 反而较低且成本翻倍，未纳入正式候选。

针对中性类误判，又做了按原视频 ID 分组的5折中性 logit 偏置交叉拟合。五折选出的偏置方向不一致，五个留出折的综合分数均下降；整体完整 Macro-F1 从0.6208降至0.6149，30%缺失平均 F1 从0.6049降至0.5997。原三种子模型本身曾使用整份 valid 选型，因此这项折外偏置实验也不是全模型的全新独立测试；它仅说明目前没有证据采纳该校准。记录在 `outputs/问题2_中性偏置交叉验证/`。

为改善高比例文本缺失，另做了一项同网络、同损失、单种子2026的训练对照，加入50%/70%长文本段与整段文本遮挡。文本缺失70%时 Macro-F1 从0.4641升至0.4833，但完整输入从0.6087降至0.6006，TAV缺失50%从0.5485降至0.5148；56个缺失场景平均从0.56382微降至0.56316，预设综合选模分数从0.53926降至0.53215，故不替换正式模型。四项文本泄漏与整段遮挡不变性检查均通过。该实验只用 train 拟合、valid 比较，没有按附件2 test 或附件3挑模型。脚本和记录分别为 `q2_text_gap_experiment.py` 与 `outputs/问题2_长文本缺失训练实验/`。这两次未成功尝试提示不宜继续在同一验证集盲调遮挡比例或阈值。

## 2. 缺失类型、比例和位置的影响

在验证集固定同一批样本与掩码位置，对7种模态组合、10%/30%/50%/70%四种连续缺失率、每种两次随机位置重复，得到56个缺失场景；再对T/A/V/AV的30%缺失补充开头、中间、末尾12个位置场景。缺失率按有效内容位置计算，原本已经全零的A/V仍保持不可观测，因此表中的实际不可用比例可能大于模拟比例。

| 缺失模态 | 10% | 30% | 50% | 70% |
|---|---:|---:|---:|---:|
| 文本 T | 0.6138 | 0.5775 | 0.5546 | 0.5091 |
| 语音 A | 0.6248 | 0.6212 | 0.6267 | 0.6332 |
| 视觉 V | 0.6249 | 0.6288 | 0.6232 | 0.6190 |
| 文本+语音 TA | 0.6118 | 0.5782 | 0.5521 | 0.4929 |
| 文本+视觉 TV | 0.6136 | 0.5726 | 0.5343 | 0.4525 |
| 语音+视觉 AV | 0.6278 | 0.6359 | 0.6252 | 0.6173 |
| 三模态 TAV | 0.6094 | 0.5735 | 0.5473 | 0.4851 |

表内均为门控蒸馏模型的 Macro-F1，按两次随机位置取均值。验证集完整输入为0.6208；56个缺失场景的平均 Macro-F1 为0.5852，普通融合为0.5579（提高0.0273）；平均 MAE 为0.6153，普通融合为0.6276（降低0.0123）。文本缺失随比例增加明显恶化；单独缺失A/V相对平缓，符合该数据上文本更主导的观察。A或AV缺失时个别数值超过完整输入，表明小样本与固定掩码有波动，**不能**解释为“缺失提升预测性能”。30%文本缺失在开头/中间/末尾的 F1 约为0.579/0.584/0.555，位置效应目前仅做探索，不作显著性结论。

![验证集缺失类型与比例](outputs/问题2_优化实验结果/图表/问题2_鲁棒性与缺失规律.png)

## 3. 完整数据增训与附件3最终候选

完整包的 train 有 16,326 条、2,249 个视频，包含附件2训练集全部 3,395 条，并新增 12,931 条训练样本。其 valid 有 1,871 条，包含附件2 valid 的728条；train 与 valid 的视频组不重叠。完整包 test 没有加载到后续训练进程，附件3的30条匿名特征虽然可与完整包 test 特征唯一匹配，但其来源标签未读取、未参与训练或选模。为此先生成只含 train/valid 的 work/full_mosei/safe_train_valid.pkl，SHA-256 为 654912d42be7055962774791723677cf7ca4785f2646bc2714927c6a546ce92f。

完整 train 拟合归一化、教师和门控蒸馏学生；完整 valid 只用于固定缺失场景的模型比较。冻结 BERT 的全量学生在完整 valid 上的完整 Macro-F1 为0.6305、连续30%缺失均值0.6176、短散点共同缺失均值0.6036，综合分数0.5629。监督微调 BERT 后重新训练 Fusion 的综合分数为0.5651，但散点缺失没有达到预设增益门槛，因此不采用它。

最后采用一个在标签评估前固定的无阈值融合规则：文本可用比例至少50%时，将监督BERT文本分类器和全量门控蒸馏学生的概率、强度各取50%；否则文本权重为20%。完整 valid 的结果为：

| 模型 | 综合分数 | 完整 Macro-F1 | 连续30%缺失均值 | 短散点TAV均值 |
|---|---:|---:|---:|---:|
| 旧三种子门控蒸馏集成 | 0.5490 | 0.6189 | 0.6041 | 0.5954 |
| 完整数据门控蒸馏学生 | 0.5629 | 0.6305 | 0.6176 | 0.6036 |
| 固定文本-三模态融合 | **0.5713** | **0.6384** | **0.6201** | **0.6139** |

这条规则不是用附件3标签调出来的；它只按文本可用比例分段，概率混合后再执行固定的极性—强度一致性规则。逐条结果在 outputs/问题2_固定文本融合附件3结果/问题2_附件3全量预测.csv，其中30条样本为负向7条、中性8条、正向15条。附件3的131个 [UNK] 且音频、视觉同位全零的位置已在BERT编码前屏蔽；重复推理、概率归一化和SHA核验均通过。由于附件3无标签，这些类别是预测结果，不是准确率。

## 4. 基础性能、图表和错误归因

| 划分与条件 | 模型 | Accuracy↑ | Macro-F1↑ | MAE↓ | Pearson↑ |
|---|---|---:|---:|---:|---:|
| 验证集完整输入 | 普通融合 | 0.6140 | 0.6007 | 0.5882 | 0.6352 |
| 验证集完整输入 | 门控蒸馏 | **0.6360** | **0.6208** | **0.5824** | **0.6420** |
| 验证集56种缺失平均 | 普通融合 | 0.5722 | 0.5579 | 0.6276 | 0.5594 |
| 验证集56种缺失平均 | 门控蒸馏 | **0.5985** | **0.5852** | **0.6153** | **0.5833** |
| 附件2留出测试完整输入 | 普通融合 | **0.6671** | **0.6375** | 0.6274 | 0.6694 |
| 附件2留出测试完整输入 | 门控蒸馏 | 0.6644 | 0.6307 | **0.6221** | **0.6705** |

留出测试集在完整输入上的 Macro-F1 比基线低0.0068；其五种30%缺失场景平均 Macro-F1 为0.6049，也略低于基线0.6073，尽管 MAE 从0.6601降到0.6526。验证集收益没有稳定迁移到留出测试，这是当前方案的主要局限，不应据此宣称鲁棒性已全面优于基线，也不应以测试集反向选择训练方案。

验证集混淆矩阵（真值为行，预测为列）为负向 `[141,38,27]`、中性 `[34,97,53]`、正向 `[48,65,225]`。中性错误率87/184≈47.3%，高于负向约31.6%和正向约33.4%。原生视觉存在不可观测位置的85条样本，分类错误率48.2%，完整可观测的643条为34.8%；这只是相关性，不能单凭该分组证明视觉缺失造成错误。强度回归在大幅情绪上明显向零收缩，图中的散点和异常案例可直接检查。最大误差样本包括 `f_ZJ7L14oYQ$_$21`（真值正向2.0，预测负向−1.389）以及 `130456$_$3`（“five stars”正向2.333，预测负向−0.621）。这些句子含有局部负面词、转折或口语停顿，提示模型可能没有抓住整句立场；是否存在原标注问题仍需结合原视频人工复核。

![验证集混淆矩阵与强度回归](outputs/问题2_优化实验结果/图表/问题2_验证集分类与强度预测.png)

## 复现与文件

运行命令见 `README_Q2.md`。已用 Python 3.12、PyTorch 2.9.0、Transformers 4.57.6、NumPy 2.2.6、scikit-learn 1.9.0、openpyxl 3.1.5、Matplotlib 3.11.2 和 RTX A6000 运行。BERT 修订版、源数据与选定模型的 SHA-256 保存在 `问题2_集成选模结论.json`；原始训练日志、随机种子、审计表和各模型预测均留在远程 `outputs/` 中。`q2_predict_ensemble.py` 从三个选定权重独立重新生成附件3的30条 CSV，已与主流程结果逐字节核对一致。

后续若继续提升，应使用按视频分组的全新留出方案或人工复核审计标记样本，不能用附件3来源标签反向调参。当前代码、配置和结果均未包含数据集压缩包；GitHub同步时只提交可复现脚本和说明。
