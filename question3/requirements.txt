# Use the existing CUDA PyTorch in conda MathE. Do not replace it with CPU torch.
numpy>=1.26,<3
scikit-learn>=1.4,<2
transformers==4.57.6
matplotlib>=3.8,<4
scipy>=1.11,<2
safetensors>=0.4
# torch>=2.6 must already be installed for the server CUDA runtime.
